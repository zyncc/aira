apiVersion: v1
kind: Service
metadata:
  name: aira-web-service
spec:
  type: NodePort
  selector:
    app: web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
